using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// ルール説明を管理する
/// </summary>
public class RuleManager : MonoBehaviour
{
    public bool IsNeedRule()
    {
        return true;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}