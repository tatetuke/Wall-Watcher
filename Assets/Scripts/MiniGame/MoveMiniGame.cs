using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// ミニゲームを管理する
/// </summary>
/// memo : MoveMiniGameはメインシーンに置き
/// 　　　ミニゲームを始めるときは、MiniGameSceneに移動する
public class MoveMiniGame : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        if(/* something */ false)
        {
            // SceneManager.LoadScene("MiniGameManager");
        }
    }
}
